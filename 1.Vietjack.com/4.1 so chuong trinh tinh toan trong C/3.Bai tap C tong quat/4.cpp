#include<stdio.h>

int main(){
	int x, y;
	printf("Nhap vao 2 so x va y: ");
	scanf("%d %d", &x, &y);
	
	if(x <2000 && x >3000)
		printf("%d", x);
	
	if(100<= y<-500);
		printf("%d", y);
		
	return 0;
}
